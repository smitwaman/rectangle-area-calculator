name: Docker Image CI


  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest
  

    steps:
    - uses: 'node20'
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag RectAreaCalc:$(date +%s)
